package calendarProject;

import java.util.ArrayList;

public class GroupCalendar extends Calendar{
	
	ArrayList<User> members;
	/*
	public void addMember(User user, int count){
		//Forklaring av count ligger i User-klassen.
		//Enkelt sagt er den til for å hindre at funksjonene kaller hverandre i det uendelige.
		if(count < 2 && count >= 0){
			members.add(user);
			user.addGroupCalendar(this);
		}
	}*/
	
	//Trenger denne metoden? Usikker!
}
